language: shell

addons:
  apt:
    packages:
    - openssh-client-7.1
    - zsh

script:
- |
  shellcheck \
    --exclude=SC2148 \
    .alias \
    .export \
    .function \
    .path \
    .profile \
    installer.sh
- |
  shfmt -i 2 -l \
    .alias \
    .export \
    .function \
    .path \
    .profile \
    installer.sh
- bats test

